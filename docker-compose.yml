version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: api_sentinel_mysql
    environment:
      MYSQL_DATABASE: api_sentinel_db
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
